import React, { StyleSheet } from 'react-native'

const TabBarStyleSheet = StyleSheet.create({
  tabContent: {
    flex: 1,
    alignItems: 'center',
  },
  tabText: {
    color: '#009999',
    margin: 50,
  }
})

export default TabBarStyleSheet