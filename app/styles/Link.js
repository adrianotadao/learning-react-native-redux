import React, { StyleSheet } from 'react-native'

const Link = StyleSheet.create({
  wrapper: {
    flexDirection: 'row',
    padding: 5,
    borderBottomWidth: 1,
    borderBottomColor: '#e9e9e9',
  },
  thumb: {
    width: 64,
    height: 64
  }
})

export default Link